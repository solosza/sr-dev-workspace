# /platform

Expansion angle — become the rails/ecosystem others build on, not just a service.

## Usage

```
/platform <business or idea>
```

## What It Does

Asks: what layer could this own so others build on top (the infrastructure, the standard, the marketplace, the API)? The picks-and-shovels-but-bigger play. Think-bigger, generative. Output = quickest view. Saved (report + ledger + venture record).

## Skill Reference

-> `.claude/skills/platform/`
