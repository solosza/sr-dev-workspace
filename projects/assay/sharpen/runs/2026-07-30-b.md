# Sharpen — meta pass (after the /expand run)

**Date:** 2026-07-30 · Reads: loop ledgers + venture records + the loop-candidates register.

## Loop-gap promotion (the self-extend test)
Register `projects/assay/loop-candidates.jsonl`:
| Candidate | Spotted | Decision |
|-----------|---------|----------|
| `/acquire` (buy an agency/book instead of build — rollup/M&A) | **1x** (this /expand run) | **QUEUED, not promoted** — recurrence threshold is >=2x. If a second run bumps into it, it gets built. |

The threshold worked: a one-off angle is *captured* but doesn't auto-become a loop. It waits for the system to bump into it again — which filters noise from real gaps.

## Engine-upgrade candidates (still queued from prior runs)
| # | Pattern | Change | Status |
|---|---------|--------|--------|
| 1 | guru-source tell under-fires | add a `guru-source / doer-vs-seller` gate | **recommend APPLY** (would've killed the AAA idea harder) |
| 2 | reachability fix is always "go warm" | warm-channel reframe prompt on the reachability gate | queued |
| 3 | governance-moat keeps winning | governance-moat sub-prompt in the recombine lens | queued |

## Verdict
- **Self-extend works end-to-end:** `/expand` captured `/acquire` → `/sharpen` read it → correctly held it (1x < 2x).
- **Recommend:** apply upgrade #1 (the guru-gate) — highest signal.

**HITL:** apply guru-gate / apply all / skip. (`/acquire` auto-builds once it recurs.)
