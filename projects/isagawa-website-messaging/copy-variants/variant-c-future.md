# Hero Copy — Variant C: Future-Forward

## Hero Headline
The framework that builds itself.

## Hero Subheadline
Describe what you want in natural language. Isagawa produces the agents, the governance, and the proof — then uses what it built to build more.

## Hero Description
Isagawa Kernel started as a minimal set of enforcement mechanisms. Through conversation, it produced its own protocol, its own commands, its own agent factory. That factory now compiles natural language into governed AI agents automatically. Each new capability the system produces extends the range of what future conversations can build. The framework is not just producing outputs. It is extending itself — and every extension is governed, attested, and permanent.

## CTA
See the kernel → | Read the story →

## Rationale
This variant leads with the self-extension capability — the most differentiated claim Isagawa can make. No competitor in the market claims their framework improves its own capabilities through use. The narrative arc (minimal kernel → factory → self-extension) mirrors the existing homepage structure but front-loads the payoff instead of building to it. References the recursive loop (system produces capabilities that extend itself) as the core differentiator.

## Self-Extension Capabilities Referenced
- System produced its own protocol from conversation
- Factory emerged from kernel-managed conversations
- Each new skill/command extends future conversation range
- 130+ backlogs, 90+ pipeline executions — all self-governed
