# Task 005: L1 - Structure + Lexicon

**Type:** TEST (L1) | **Gates:** DO-05

## Action
ONE Python script: package structure matches data-objects.md canonical layout (parse the doc's structure section); extended vocab lexicon over branch diff = 0 hits; single-root import audit (no bare 'from data_objects' - must be 'from _reference.data_objects'); py_compile all.

## Acceptance
Exit 0. Red: fix then /kernel/learn.
