# Task 004: L2 - Fixture + Parametrize Semantics

**Type:** TEST (L2) | **Gates:** DE-04

## Action
ONE script: fixture scope check (function/session appropriateness per doc); no hardcoded credentials (grep for password literals beyond the documented dev default); parametrize test IDs are variant keys not raw SP/table names.

## Acceptance
All compliant, exit 0. Red: fix then /kernel/learn.
