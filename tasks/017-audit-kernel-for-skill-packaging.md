# Audit Kernel for Skill Packaging

## Context
Map every file that must exist for the Isagawa Kernel to function. Identify what's currently manual setup vs what domain-setup generates. This audit determines exactly what the kernel spec needs to produce.

## Dependencies
- None (first task in meta-spec sequence)

## Requirements

### Audit the canonical kernel repo
Read every file in `D:\my_ai_projects\isagawa-kernel` and categorize:

**Category 1: Core kernel files (must exist for kernel to work)**
- `CLAUDE.md` — the kernel's rules, loop, commands reference
- `.claude/commands/kernel/` — all kernel commands (session-start, anchor, learn, complete, fix, reset, domain-setup, autonomous-cycle)
- `.claude/hooks/` — gate enforcer, hook scripts
- `.claude/skills/kernel-domain-setup/` — the domain-setup skill (11 steps)
- `.claude/skills/autonomous-cycling/` — the cycling skill
- `.claude/state/` — state file templates
- `settings.local.json` — hook registration

**Category 2: Generated by domain-setup (Layer 3 output)**
- `.claude/protocols/[domain]-protocol.md`
- `.claude/lessons/lessons.md`
- Domain-specific commands, hooks

**Category 3: Domain spec files (Layer 2 — separate SKU)**
- `.claude/skills/[domain]-guidance/`

### Cross-reference with cognitive-agent
Compare canonical kernel against `D:\my_ai_projects\project_test_repos\cognitive-agent`:
- Files present in cognitive-agent but not in canonical kernel
- Files generated by domain-setup vs manually placed
- Hook registration differences in `settings.local.json`

### Document the audit
Write findings to: `D:\my_ai_projects\project_test_repos\sr_dev_test\research\017-kernel-audit.md`

Structure:
```markdown
# Kernel Audit — Skill Packaging

## Category 1: Core Kernel Files (kernel spec must produce these)
| File | Purpose | Currently Manual? |

## Category 2: Generated by Domain-Setup (NOT kernel spec's job)
| File | Purpose | Generated By |

## Category 3: Domain Spec Files (separate SKU)
| File | Purpose |

## Bootstrap Sequence
1. What must exist BEFORE domain-setup can run?
2. What does domain-setup create?
3. What does the domain spec provide?

## Key Insight
domain-setup is the constant primitive — it always exists.
The kernel spec is just the first domain spec it consumes.

## Gaps / Questions
```

## Output
- `sr_dev_test/research/017-kernel-audit.md`

## Validation (check ALL before completing)
- [ ] Audit file exists at expected path
- [ ] All three categories documented with specific file paths
- [ ] Bootstrap sequence clearly defined
- [ ] Cross-reference with cognitive-agent completed
- [ ] No unknowns left undocumented

## Completion Signal
When ALL validation checks pass, invoke `/kernel/complete`.
