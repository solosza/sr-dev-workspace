# Write generate-gate-tasks.md

## Context
Step-11 sub-reference: Parse gate-contract.md, create one task per gate, task template, ordering

## Type
BUILD

## Dependencies
- 024 (validation directory exists)

## Phase Gate
- [ ] `C:/Users/solos/my_ai_projects/domain-spec-factory/.claude/skills/spec-factory/references/validation/` directory exists

## Requirements
- Write `C:/Users/solos/my_ai_projects/domain-spec-factory/.claude/skills/spec-factory/references/validation/generate-gate-tasks.md`
- Must be self-contained with full implementation detail
- Content: Parse gate-contract.md, create one task per gate, task template, ordering

## Acceptance Criteria
- [ ] `generate-gate-tasks.md` exists at validation path (verify: file_exists)

## Gates Satisfied
S11-05

## Completion Signal
When ALL acceptance criteria are met, invoke `/kernel/complete`.
