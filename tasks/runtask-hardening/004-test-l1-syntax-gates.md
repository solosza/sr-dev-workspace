# Task 004: L1 - Syntax + Regression Greps

**Type:** TEST (L1) | **Gates:** RH-04

## Action
ONE script: bash -n on the patched run-task.sh; greps confirm preserved features: LOCK_FILE handling, KERNEL_AGENT_ID export in run_claude, IS_WORKTREE detection, move_to_done, pre-check block, [EMPTY-RETRY] marker present, heartbeat write present.

## Acceptance
Exit 0. Red: fix then /kernel/learn.
