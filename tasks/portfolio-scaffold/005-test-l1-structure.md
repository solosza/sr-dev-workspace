# Task 005: L1 - Structure + IP-Safety Greps

**Type:** TEST (L1) | **Gates:** PF-05 (+PF-02 static)

## Action
ONE Python script: parse index.html - all 7 section ids + matching nav hrefs; grep repo for kernel-internal terms (session_state, sr_dev_workflow, universal-gate-enforcer, KERNEL_AGENT_ID, anchor ceremony, run-task.sh, hook names) = 0 hits; absolute-claims grep ('100%%', guaranteed, unbypassable, zero drift) = 0 hits; 'YOUR NAME' placeholder absent; OG tags present.

## Acceptance
Exit 0. Red: fix then /kernel/learn.
