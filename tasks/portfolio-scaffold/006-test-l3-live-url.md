# Task 006: L3 - Live URL (GATE)

**Type:** TEST (L3) - GATE TASK: skip never waives (lesson #39).
**Gates:** PF-06

## Action
ONE script: resolve the Pages URL (gh api pages status), poll HTTP GET up to 10 minutes for 200, then assert: browser title present, all 7 nav anchors in HTML, hero shows the real name. Report the final public URL prominently.

## Acceptance
Live 200 + content asserts, URL in report. Red: fix then /kernel/learn.
