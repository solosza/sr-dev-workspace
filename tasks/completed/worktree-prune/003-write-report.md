# 003 — Write prune report

## Action
Write `projects/worktree-prune/prune-report.md`: count before/after, list removed, list skipped (KEEP) with reasons.

## Acceptance
- `projects/worktree-prune/prune-report.md` exists with before/after counts + removed/skipped lists.
