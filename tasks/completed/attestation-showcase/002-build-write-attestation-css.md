# 002 — Write attestation.css

## Type
BUILD

## Requirements
Create `attestation.css` in `D:\my_ai_projects\isagawa-co.github.io\`. Must match the existing dark theme design system from `styles.css` (CSS variables, STIX Two Text font, pure black bg, warm off-white accent). Reuse the same variable names.

Sections to style:
- Nav (reuse from feed.css pattern)
- Hero section with headline + terminal block
- Problem statement section
- How-it-works flow diagram (5 steps with connector lines)
- Bundle anatomy (annotated JSON code block)
- Drop-in setup (3-step numbered list with code blocks)
- Tech stack badges
- Results / live stats section
- "Built by the loop" opener line
- Responsive: mobile-first, breakpoints at 768px and 480px

## Acceptance Criteria
- [ ] `D:\my_ai_projects\isagawa-co.github.io\attestation.css` exists
- [ ] Uses CSS variables from the design system (--bg-primary, --text-primary, --font-heading, etc.)
- [ ] Under 300 lines (hook limit)
