# 004 — Write Feed Page CSS

**Type:** BUILD
**Depends on:** 003

## Requirements
Write `D:\my_ai_projects\isagawa-co.github.io\feed.css` with styles for the feed page:

- Dark theme matching existing `styles.css` (background, fonts, colors)
- `.feed-entry` — card-style entries with border, padding, spacing
- Category color classes: `.cat-kernel`, `.cat-market`, `.cat-domain`, `.cat-test` — each with a distinct accent color (left border or tag color)
- `.feed-header`, `.feed-footer` — styled header/footer text
- `.feed-meta` — timestamp, task count, intent revisions in a row
- `.rekor-link` — styled external link
- Mobile responsive (stack on narrow screens)
- Nav bar styling consistent with main site

## Acceptance Criteria
- [ ] `D:\my_ai_projects\isagawa-co.github.io\feed.css` exists
- [ ] Contains `.feed-entry`, `.cat-kernel`, `.cat-market`, `.cat-domain`, `.cat-test` classes
- [ ] Contains `@media` responsive rules
