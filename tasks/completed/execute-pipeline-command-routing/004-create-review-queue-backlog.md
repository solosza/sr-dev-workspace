# Task 004: Create Review-Queue Backlog

## Objective
Create a backlog item for the `/kernel/review-queue` command using `/kernel/backlog`. This backlog will serve as the integration test for the new routing logic.

## Instructions
1. Read the research report: `projects/velocity-management-research/final-report.md`
2. Create backlog via `/kernel/backlog`:
   - Title: Build /kernel/review-queue command
   - Scope: BUILD
   - Deliverable: `/kernel/review-queue` command + `review-status.json` state file
   - Location: workspace:.claude/commands/kernel/ and .claude/skills/review-queue/
   - Include requirements from the research report (review queue, state file, accept/iterate/reject actions)
   - Priority: High

## Acceptance Criteria
- [ ] Backlog created in docs/backlog/ with correct format
- [ ] Deliverable clearly identifies this as a command build (triggers routing)
- [ ] Requirements trace back to velocity-management-research report
