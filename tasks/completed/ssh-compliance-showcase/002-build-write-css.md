# 002 — Write SSH Compliance CSS

## Type
BUILD

## Description
Write `ssh-compliance.css` in `D:\my_ai_projects\isagawa-co.github.io`. Must match the existing isagawa.co design system (dark theme, radial-gradient bg, grain overlay, STIX Two Text serif, SF Mono monospace, warm off-white accent #fcf9f3, evidence cards with hover effects, flow cards, clamp() responsive typography).

Use the same CSS variable definitions and patterns as `attestation.css` and `styles.css`. Typography scale must match the homepage (hero h1 at `clamp(3.5rem, 8vw, 7rem)`, section h2 at `clamp(2.75rem, 5.5vw, 5rem)`, section padding at `clamp(8rem, 18vh, 18rem)`).

## Acceptance Criteria
- [ ] `D:\my_ai_projects\isagawa-co.github.io\ssh-compliance.css` exists
- [ ] Contains CSS variables matching the design system
- [ ] Contains styles for: nav, hero, page sections, evidence cards, flow cards, badges, footer
- [ ] Under 200 lines
