# 008 — Write Generation Skill Design Sketch

## Type
BUILD

## Description
Write an architecture design sketch for generation skills in the kernel.

## Requirements
- Read analysis from task 005 (generation gap)
- Write `projects/kernel-architecture/generation-skill-design.md` with:
  - Input contract: what structured data a generation skill receives
  - Output contract: what it produces
  - Pipeline stages: how generation flows (like extraction has navigate → screenshot → extract)
  - Composability: how it chains with extraction skills
  - Example: "site-builder" skill that takes tokens + content + architecture → HTML + CSS

## Acceptance Criteria
- [ ] `projects/kernel-architecture/generation-skill-design.md` exists with architecture

## Gates
BUILD-03, FUNC-04
