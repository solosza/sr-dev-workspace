# Task 005: L1 - Structure + Backward-Compat
**Type:** TEST (L1) | **Gates:** DW-05
## Action
ONE script: modified step files + any lib present; py_compile/bash -n as applicable; assert the no-depends_on path still yields a single wave (backward compatibility - a flat backlog list behaves as today).
## Acceptance
Exit 0. Red: fix then /kernel/learn.
