# 012 — L2: Verify run-task.sh Bash Syntax

## Type
TEST

## Requirements
- Run `bash -n run-task.sh` to verify the modified script has no syntax errors
- Run `bash -n lib/common.sh` to verify the shared library still parses

## Acceptance Criteria
- [ ] `bash -n run-task.sh` exits 0
- [ ] `bash -n lib/common.sh` exits 0
