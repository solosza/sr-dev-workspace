# Task 004: L2 - Contract Semantics

**Type:** TEST (L2) | **Gates:** SR-04

## Action
ONE script (AST body-scoped, docstring-excluded, decorator-aware): except-reraise classification; constructor purity; imports audit (task modules only - no data_objects/interfaces imports); typed-return runtime verification with mocked tasks; when-NOT-to-create docstring grep.

## Acceptance
All compliant, exit 0. Red: fix then /kernel/learn.
