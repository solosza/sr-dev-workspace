# L2 Test — Run Pytest

## Type
TEST

## Description
Install dependencies and run the full pytest suite to verify all code compiles and tests pass.

## Requirements
1. `cd D:\my_ai_projects\fraud-detection-app`
2. `pip install -r requirements.txt`
3. `python -m pytest tests/ -v`
4. All tests must pass (exit code 0)

## Acceptance Criteria
- [ ] `cd D:/my_ai_projects/fraud-detection-app && pip install -r requirements.txt` exits 0
- [ ] `cd D:/my_ai_projects/fraud-detection-app && python -m pytest tests/ -v` exits 0
