# Task 004: L2 - Contract Semantics + Negative Path
**Type:** TEST (L2) | **Gates:** SI4-03, SI4-05
## Action
ONE script (AST body-scoped, docstring-excluded, decorator-aware): every except re-raises or is a documented bool state-check; then negative path LIVE - construct the interface pointed at the booted 220 SOAP service, call a bad operation name / unreachable WSDL, assert the fault PROPAGATES to caller AND a log line was emitted before the raise.
## Acceptance
All classified compliant + propagation proven, exit 0. Red: fix then /kernel/learn.
