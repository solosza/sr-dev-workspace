# 002 — Build clean commit recommendation

## Action
From the classification, produce: (a) the recommended commit set (REAL WORK files only), (b) a concrete commit message, (c) a `.gitignore` addition that would stop the NOISE recurring, (d) an explicit exclude list (NOISE + DO-NOT-COMMIT).

## Acceptance
- Recommendation captured (commit set, message, .gitignore suggestion, exclude list).
- Still READ-ONLY — recommendation only, nothing executed.
